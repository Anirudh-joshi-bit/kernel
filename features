+usart driver (printf)
+3 states running, ready, suspend
+user space, kernel space separation
+kernel envoke regularly by systick interrupt
+context switching in pendsv
+system call (svc)
+ready queue
+round robin schedular
+multiple processes (not only 2)
+fault diagnosis (logging, pc)
+kernel panic (if fault happens in kernel code)
+user process suspend (if fault occures in user process)
+hardfault handler (logging, pc, instruction, halt)
+mpu configured....
    accessing kernel space in user space => memmanage fault -> terminates process 
    accessing illegal memory in user -> mem + bus fault => hardfault 
    accessing illegal memory in kernel-> bus fault =>  kernel panic

* improvement >now system is crashing if user process accesses ilegal memo-> resolve this
*   bug     -> when user access kernel code -> memorymanage fault but after few seconds 
            ie after about 100s prints, Hardfault is triggering !!! (dont know why)
            
            suspection ->
                        1. as after memmanage fault -> the process is terminated, and 
                        after some 100s context switching, hardfault triggered ->
                        something might be wrong with the CIRCULAR QUEUE 
                        
                        2. hardfault handler logs what was the pc, inst when Hardfault
                        triggered ... pc was in PRINTF function ... there might be some 
                        illegal memory access 



* semaphore added ....  
* added waiting state ....
    implementation -> if process 1 locks the semaphore ->   < semaphore.value -- >
                        process 2 wants to access the CS -> as semaphore is already
                        locked -> process will have to wait =>change the running 
                        process state from RUNNING STATE to WAITINT STATE , 
                        push the process into < semaphore waiting queue >,
                        now call PendSV_Handler to preemption ....
                        
                        inside the schedular, donot push waiting state processes 
                        into the ready queue... now waiting processes cannot get the 
                        cpu until they are removed from the waiting queue and pushed 
                        into the ready queue !!!


        semaphore unlock -> <semaphore.value ++>    if after increament, the value is
                        1, then semaphore is unlocked, meaning we need to pick one
                        process from <semaphore.waiting_queue> (if not empty),
                        pop from the <semaphore.waiting_queue> ,
                        change the state from WAITING_STATE to READY_STATE,
                        push the process to ready_queue
